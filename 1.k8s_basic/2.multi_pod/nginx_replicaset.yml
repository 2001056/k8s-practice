apiVersion: apps/v1
kind: ReplicaSet
metadata:
  name: nginx-replicaset
  namespace: chan-ns
spec:
  replicas: 2
  selector:
  # 아래의 label은 replicaset이 관리해야 할 파드를 결정하는데 사용
    matchLabels:
      app: my-nginx
  template:
    metadata:
    # 아래의 label은 service에서 라우팅 할 대상이 되는 label. service의 selector.app과 일치
      labels:
        app: my-nginx
    spec:
      containers:
      - name: nginx
        image: nginx
        ports:
        - containerPort: 80
---
apiVersion: v1
kind: Service
metadata:
  name: my-service
  namespace: chan-ns
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 80
  selector:
    app: my-nginx